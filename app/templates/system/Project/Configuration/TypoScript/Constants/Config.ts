config {

    # Development
    devURL          = <%= _.slugify(websiteName) %>.localhost

    # Stage Url
    stageURL        = stage.tld

    # Set TYPO3 Base URL
    wwwURL          = online.tld
    
}
